@User.route('/user/<int:user_id>')
def user_profile(user_id):

@User.route('/user/<int:user_id>/watchlist')
def user_watchlist(user_id):

@User.route('/user/<int:user_id>/reviews')
def user_reviews(user_id):

